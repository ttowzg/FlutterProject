import 'package:flutter/material.dart';

class AppColors {
  // Vinho oficial da UFOP (aproximado da identidade visual)
  static const Color primaryVinho = Color(0xFF731B3E); 
  
  // Cinza oficial
  static const Color secondaryCinza = Color(0xFF5A595D);

  // Cores de apoio
  static const Color success = Color(0xFF2E7D32); 
  static const Color onSuccess = Colors.white;
  static const Color surface = Color.fromARGB(255, 245, 245, 245);
  static const Color error = Color(0xFFB00020);
}